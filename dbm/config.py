dbname = 'parameters'
hostname = 'localhost'

adminconfig = {
    'user' : 'user',
    'password' : 'pass',
    'host' : hostname,
    'database' : dbname
}

read_only_config = {
    'user' : 'user',
    'password' : 'pass',
    'host' : hostname,
    'database' : dbname
}
